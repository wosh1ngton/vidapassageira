# ================================================
# MYSQL (Banco de Dados)
# ================================================
MYSQL_DATABASE=vp
MYSQL_ROOT_PASSWORD=SuaSenhaSegura123!
DATABASE_URL=jdbc:mysql://mysql:3306/vp?useTimezone=true&serverTimezone=UTC
DATABASE_USERNAME=root
DATABASE_PASSWORD=SuaSenhaSegura123!

# ================================================
# SPRING BOOT (Backend)
# ================================================
SPRING_PROFILES_ACTIVE=prod
SERVER_PORT=8080

# ================================================
# KEYCLOAK (Autenticação)
# ================================================
# Admin Credentials
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=SuaSenhaAdminKeycloak!

# Client Configuration
KEYCLOAK_USERNAME=admin
KEYCLOAK_PASSWORD=admin
KEYCLOAK_CLIENT_ID=admin-cli
KEYCLOAK_TARGET_REALM=VP

# URLs e Endpoints (ajuste para seu domínio)
KEYCLOAK_SERVER_URL=http://keycloak:8080
KEYCLOAK_ISSUER_URI=https://sharedbill.com.br/realms/VP
KEYCLOAK_JWT_SET_URI=http://keycloak:8080/realms/VP/protocol/openid-connect/certs
SPRING_SECURITY_OAUTH2_RESOURCESERVER_JWT_ISSUER_URI=https://sharedbill.com.br/realms/VP

# Hostname Configuration (ajuste para seu domínio)
KC_HOSTNAME=sharedbill.com.br
KC_HOSTNAME_ADMIN=sharedbill.com.br
KC_HOSTNAME_STRICT=true
KC_HTTP_ENABLED=true
KC_PROXY=edge
KC_LOCALE=pt-BR

# ================================================
# DEEPSEEK API (Integração IA)
# ================================================
DEEPSEEK_API_KEY=sk-sua-chave-deepseek-aqui

# ================================================
# SEGURANÇA
# ================================================
API_TOKENCADASTROUSUARIO=TokenSeguroParaCadastroDeUsuarios123
