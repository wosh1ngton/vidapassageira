export interface DestinoCreateDTO {
  nome: string;
  descricao: string;
  localizacao: string;
}

export interface DestinoResponseDTO {
  id: number;
  nome: string;
  descricao: string;
  localizacao: string;
}

